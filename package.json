{
  "scripts": {
    "postinstall": "npx gulp",
    "lint": "npx eslint src/js",
    "chrome": "npm run make-chrome && web-ext run --source-dir ./src/ --target=chromium",
    "firefox": "npm run make-firefox && web-ext run --source-dir ./src/",
    "make-chrome": "npx json-merger -p -am concat -o src/manifest.json manifest.json manifest-chrome.json",
    "make-firefox": "npx json-merger -p -am concat -o src/manifest.json manifest.json manifest-firefox.json",
    "build-chrome": "npm run make-chrome && npx web-ext build -n {name}-chrome-{version}.zip -o -s src",
    "build-firefox": "npm run make-firefox && npx web-ext build -n {name}-firefox-{version}.zip -o -s src",
    "build-all": "npm run build-chrome && npm run build-firefox"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "clipboard": "^2.0.11",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "gulp": "^4.0.2",
    "json-merger": "^1.1.10",
    "prettier": "^3.0.3",
    "web-ext": "^7.8.0"
  }
}
